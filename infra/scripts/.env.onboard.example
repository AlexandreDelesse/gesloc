# Gesloc onboarding script config — copy to .env.onboard and fill in values
# .env.onboard is gitignored — never commit secrets

KC_URL=https://auth.example.com
KC_ADMIN_USER=admin
KC_ADMIN_PASS=changeme
DATABASE_URL=postgres://gesloc:changeme@localhost:5432/gesloc
BREVO_API_KEY=xkeysib-...
APP_URL=https://app.gesloc.fr
